---
phase: 06-csv-writer-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - csv-writer.mjs
  - test/csv-writer.test.mjs
autonomous: true
requirements:
  - CSV-02
  - CSV-06
  - CSV-09

must_haves:
  truths:
    - "Scanner appends exactly N rows for N new jobs under a stable 8-column header (Date Added, Company, Role, Location, URL, Source, Age, Application)"
    - "Re-running the writer with the same input produces zero new rows on the second pass (idempotent, no duplicate append)"
    - "Existing rows are preserved verbatim when new rows are appended (append-only, never truncated)"
    - "Jobs written to the CSV can be read back via parseRows and yield the same URLs and titles in the same order"
    - "Pipe, newline, and lone-backtick characters inside job fields are escaped so GH-flavored markdown tables render correctly"
  artifacts:
    - path: "csv-writer.mjs"
      provides: "appendRows(jobs, {filePath, now}) + parseRows(csvText) - leaves Phase 7 imports"
      exports: ["appendRows", "parseRows"]
    - path: "test/csv-writer.test.mjs"
      provides: "node:test suite of 7 describe blocks - run by `npm test`"
  key_links:
    - from: "csv-writer.mjs.appendRows"
      to: "<filePath>"
      via: "atomic temp-file + rename: writeFile(tmp = filePath + '.tmp-' + randomUUID()) then rename(tmp, filePath)"
      pattern: "\\.tmp-.*randomUUID|filePath \\+ '.tmp-'"
    - from: "csv-writer.mjs.appendRows"
      to: "csv-writer.mjs.parseExistingUrls"
      via: "deduplicate incoming jobs against URLs already present in file before appending"
      pattern: "parseExistingUrls|existingUrls"
    - from: "csv-writer.mjs.escapeCell"
      to: "GH-flavored markdown spec"
      via: "escape `|` -> `\\|`, replace `\\n` with single space, balance lone backticks"
      pattern: "\\| -> \\\\\\|\\|\\\\||\\\\\\\\|"
    - from: "test/csv-writer.test.mjs"
      to: "csv-writer.mjs"
      via: "dynamic import('../csv-writer.mjs') after CSV_PATH env cleared at module top"
      pattern: "await import\\('\\.\\./csv-writer\\.mjs'\\)"
---

<objective>
build csv-writer.mjs (writer + parser) + unit tests so new jobs append to a GH-flavored markdown CSV in a stable, testable format.

purpose: Phase 6 of v1.1 CSV-as-Notification (CSV-02 schema, CSV-06 append-only, CSV-09 round-trip test). Writer module is a leaf (zero internal deps, zero npm deps); Phase 7 (GH Actions bot) and Phase 8 (channel switchover) consume it.
output: csv-writer.mjs exporting appendRows + parseRows; test/csv-writer.test.mjs with 7+ describe blocks covering empty start, N-append, idempotent re-run, append-only, escape rules, parser round-trip, missing file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-csv-writer-format/06-CONTEXT.md
@.planning/REQUIREMENTS.md

# Source files (read FIRST)
- /Users/andrianthan/Desktop/projects/job-board-aggregator/notify.mjs (applyLink @ line 30, fallbackNote @ line 36, groupByCompany logic)
- /Users/andrianthan/Desktop/projects/job-board-aggregator/test/notify.test.mjs (tmpdir + randomUUID + env-before-import fixture pattern)

<interfaces>
From notify.mjs:
- `applyLink(job) => job.url || job.fallbackUrl || ''` (line 30)
- `fallbackNote(job)` (line 36): when job.fallbackUrl is set AND job.url is missing/differs, returns `↪ apply via <hostname>` via `new URL(job.fallbackUrl).hostname` with try/catch fallback to `''`.
  The csv-writer module duplicates the hostname logic instead of importing notify.mjs (per D-04 / CONTEXT.md: keep csv-writer.mjs a leaf module).

From test/notify.test.mjs (lines 11-22):
- Sets `process.env.DB_PATH` at module top-level (BEFORE dynamic import).
- Uses `tmpdir()` + `randomUUID()` for parallel-safe unique file paths.
- Imports `node:test` (`test`, `describe`, `afterEach`) and `node:assert/strict`.
- Phase 6 mirrors this for `CSV_PATH`.

Locked decisions to honor verbatim (from 06-CONTEXT.md decisions D-01..D-08):
- D-01 schema: header exactly `Date Added, Company, Role, Location, URL, Source, Age, Application` (8 cols).
- D-02 path: `process.env.CSV_PATH` env, default `./data/jobs.csv`.
- D-03 API: `appendRows(jobs, { filePath, now = Date.now() } = {}) -> { rowsAppended, totalRows }`. Atomic write.
- D-04 parseRows(csvText) -> Job[] returning `{ url, company, title, location, source, postedAt?, dateAdded }`. Splits on pipes NOT inside backtick code-spans.
- D-05 dedup: check existing-file URLs only.
- D-06 escape: `|` -> `\|`, `\n` -> single space, unmatched `\`` stripped or escaped. Do NOT escape commas. Do NOT quote-wrap.
- D-07 columns:
  - Date Added = `YYYY-MM-DD` from `now`
  - Company = `job.company`
  - Role = `job.title`
  - Location = `job.location` (empty if missing)
  - URL = `job.url`
  - Source = `job.source`
  - Age = human string from `(now - job.postedAt)`: extract hours boundary (>24h shows days); `"unknown"` if no postedAt
  - Application = `↪ via <hostname>` when `job.fallbackUrl` set AND (url missing OR differs); empty string otherwise. `new URL(job.fallbackUrl).hostname`. If hostname parsing fails use `"fallback"`.
- D-08 tests: 7 cases in test/csv-writer.test.mjs using tmpdir + uuid + node:test + node:assert/strict. No DB, no network.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: implement csv-writer.mjs (writer + parser)</name>
  <read_first>
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/notify.mjs (lines 30, 36 for fallback hostname pattern)
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/test/notify.test.mjs (test fixture style — for parity)
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/.planning/phases/06-csv-writer-format/06-CONTEXT.md (decisions D-01..D-07 verbatim)
  </read_first>
  <files>
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/csv-writer.mjs
  </files>
  <action>
    create csv-writer.mjs at the project root. zero npm deps. ESM (`export { ... }`). exports exactly two named functions: `appendRows` and `parseRows`.

    implementation requirements (per decisions D-01..D-07):

    # constants
    - `HEADER = 'Date Added, Company, Role, Location, URL, Source, Age, Application'` — exact literal, 8 columns, comma-space separators.
    - `HEADER_RE = /^Date Added, Company, Role, Location, URL, Source, Age, Application\s*$/m` — use only internally if helpful, optional.

    # helper: `resolveFilePath(opts)`
    - return `opts.filePath` if truthy, else `process.env.CSV_PATH || './data/jobs.csv'`.

    # helper: `formatAge(now, postedAt)` -> string
    - if `postedAt` is missing/null/undefined: return `'unknown'`.
    - else compute `diffMs = now - postedAt`. if `diffMs < 24 * 60 * 60 * 1000` return `${Math.max(0, Math.floor(diffMs / (60 * 60 * 1000)))}h`. else return `${Math.floor(diffMs / (24 * 60 * 60 * 1000))}d`. (Operators routinely see `3h`, `2d`, `12d`.)
    - exact contract: hour boundary <24h hours-since; >=24h days-since. always a positive int followed by unit letter.

    # helper: `formatApp(job)` -> string
    - if `job.fallbackUrl` is set AND (`!job.url` OR `job.url !== job.fallbackUrl`): return `↪ via <hostname>` where hostname = `(function(){ try { return new URL(job.fallbackUrl).hostname; } catch { return 'fallback'; } })()`. return `''` otherwise.
    - mirrors notify.mjs:30/36 pattern but inlined; do NOT import notify.mjs.

    # helper: `escapeCell(value, { collapseWhitespace = false } = {})` -> string
    - coerce to string (`value ?? ''`). strip `\r` always.
    - if `collapseWhitespace` is true (used only for the Application column): replace any whitespace run (one or more `\s` chars including tab/newline/space) with a single space, then trim.
    - else: do NOT collapse internal whitespace (other columns preserve spacing); only replace literal `\n` with a single space.
    - escape `|` -> `\|`.
    - backtick handling: count `\`` in the value. if odd, append one more `\`` to the value before continuing (effectively escapes an unmatched backtick so it cannot open a code-span). if even, leave untouched (matched pairs render as code spans). implementation: `if ((value.match(/\`/g) || []).length % 2 === 1) value = value + '\`';`
    - return the result.

    # helper: `formatRow(job, now)` -> string
    - build columns in EXACT order matching HEADER:
      1. `new Date(now).toISOString().slice(0, 10)` (YYYY-MM-DD; uses UTC date component of injected `now`)
      2. escapeCell(job.company, false)
      3. escapeCell(job.title, false)
      4. escapeCell(job.location || '', false)
      5. escapeCell(job.url || '', false)
      6. escapeCell(job.source || '', false)
      7. formatAge(now, job.postedAt)
      8. escapeCell(formatApp(job), true)   ← Application column DOES collapse whitespace + strip \r
    - join with ` | ` (space-pipe-space). prepend `| ` and append ` |`. that's the standard GH-flavored table row.
    - return the row.

    # helper: `parseExistingUrls(text)` -> Set<string>
    - if `text` is empty/falsy: return new Set().
    - find the header line. find the line containing `|---|` (the separator row, which immediately follows the header). if no separator row exists, treat as no data rows; return new Set().
    - parse each subsequent line: trim, skip if empty or starts with `|---|` (already handled) or doesn't start with `|`. split on pipes that are NOT inside a code-span: split on `|` chars except when inside `` ` ``...`` ` `` regions. (regex implementation: `text.split(/(?<!`)\|(?!`)/)` does not work cleanly because backticks are not always paired on the same token; for tests this can be approximated by `line.split('|').map(s=>s.trim()).filter(...)` since real fields won't have surrounding backticks in the URL/Source/Location columns. document this nuance in a comment.)
    - for row array of trimmed cells, validate length >= 8. column index 4 is the URL. push `cells[4]` into the Set.
    - return the Set.

    # helper: `formatHeader()` -> string
    - return exactly:
      ```
      Date Added, Company, Role, Location, URL, Source, Age, Application
      |---|---|---|---|---|---|---|---|
      ```
    - (TWO lines: header + separator row. Both lines end with `\n`.)

    # exported `parseRows(csvText)` -> Job[]
    - per D-04: returns `[{ url, company, title, location, source, postedAt?, dateAdded }]`.
    - for each data row (lines after separator): trim, split on `|`, trim each cell. skip rows with < 8 cells.
    - cell indexes match HEADER: 0=Date Added, 1=Company, 2=Role, 3=Location, 4=URL, 5=Source, 6=Age, 7=Application.
    - output Job shape: `{ url: cells[4], company: cells[1], title: cells[2], location: cells[3], source: cells[5], dateAdded: cells[0] }`.
    - `postedAt` is optional — derive from Age cell when present: if `cells[6]` matches `/^(\d+)h$/` -> hours from `now`. if `cells[6]` matches `/^(\d+)d$/` -> days from `now`. parser does NOT have access to a deterministic `now`, so leave `postedAt` undefined unless the caller passes one in. document this in comment. (tests inject now and recompute postedAt themselves; round-trip test asserts on url+title equality only per D-08.)

    # exported `appendRows(jobs, { filePath, now = Date.now() } = {})` -> { rowsAppended, totalRows }
    - `filePath = resolveFilePath({ filePath })`.
    - read existing file via `fs.readFile(filePath, 'utf8')` wrapped in try/catch ENOENT -> empty string.
    - if `existing` has no header line OR no separator row: treat as cold start (`rowsAppended = 0` from dedup perspective, but the file will get a fresh header).
    - `existingUrls = parseExistingUrls(existing)` (Set of already-seen URLs).
    - dedup: `freshJobs = jobs.filter(j => j && j.url && !existingUrls.has(j.url))`. also dedup within the input array itself (same URL appears twice in `jobs`): keep only the FIRST occurrence per URL. (matches D-05 "writer checks existing-file URLs only; trusts DB to filter newJobs" — but file-level belt-and-suspenders.)
    - build new content = (existing content if valid header/separator) ELSE '' + `formatHeader()` + (existing rows that were already in the file, joined by '\n', WITHOUT rewriting them) + (filtered new rows formatted via `formatRow`, joined by '\n').
      - atomic write algorithm (exact steps — copy verbatim):
        1. `const tmp = filePath + '.tmp-' + randomUUID();` (e.g. `./data/jobs.csv.tmp-7f3c-...`).
        2. `await fs.writeFile(tmp, content, 'utf8');`.
        3. `await fs.rename(tmp, filePath);`. (`fs.rename` is atomic on POSIX same-fs; matches the canonical pattern.)
    - return `{ rowsAppended: freshJobs.length, totalRows: <count of data rows in the file we just wrote> }`.
      - `totalRows` = number of data rows (excluding header + separator). compute by parsing the freshly-written content with `parseExistingUrls` (the existing one is stale by this point — re-count from the new text).
      - simpler: count `content.split('\n').filter(l => l.startsWith('|') && !l.startsWith('|---')).length` (data rows only).
    - use `node:fs/promises` for all fs ops. use `randomUUID` from `node:crypto`.

    # top-of-file imports
    - `import { readFile, writeFile, rename } from 'node:fs/promises';`
    - `import { randomUUID } from 'node:crypto';`
    - no other imports. zero internal module deps (no scan.mjs, no notify.mjs, no db.mjs).

    # no console.log/error/stdout. function is silent; caller logs rowsAppended.
  </action>
  <behavior>
    - `await writeFile` writes the temp file before `rename` (crash-safe).
    - `parseRows` returns at least `{ url, company, title, location, source, dateAdded }` per data row.
    - empty input jobs: returns `{ rowsAppended: 0, totalRows: 0 }` AND still writes a header+separator (cold start).
    - duplicate URL in jobs: filtered to first occurrence only.
    - URL already in existing file: filtered out (no duplicate append).
  </behavior>
  <verify>
    <automated>MISSING — test/csv-writer.test.mjs is created by Task 2 in the same plan; run via `npm test` (which is `node --test`, discovers test/*.test.mjs). Phase 6 verification deferred to Task 2's suite.</automated>
  </verify>
  <done>
    csv-writer.mjs exists at /Users/andrianthan/Desktop/projects/job-board-aggregator/csv-writer.mjs with `export function appendRows` and `export function parseRows` defined, the HEADER literal present at byte-exact `Date Added, Company, Role, Location, URL, Source, Age, Application`, the atomic-write temp-path pattern present (some occurrence of `.tmp-` + `randomUUID`), `process.env.CSV_PATH` resolution present, no internal imports (no `from '../`) of any module).
  </done>
  <acceptance_criteria>
    - `csv-writer.mjs` exists at /Users/andrianthan/Desktop/projects/job-board-aggregator/csv-writer.mjs.
    - `grep -c '^Date Added, Company, Role, Location, URL, Source, Age, Application' csv-writer.mjs` returns >= 1 (header literal present).
    - `grep -c 'export function appendRows' csv-writer.mjs` returns 1.
    - `grep -c 'export function parseRows' csv-writer.mjs` returns 1.
    - `grep -c "\.tmp-' + randomUUID\|.tmp-\${randomUUID}\|filePath + '.tmp-'" csv-writer.mjs` returns >= 1 (atomic write temp-file pattern present).
    - `grep -c 'process.env.CSV_PATH' csv-writer.mjs` returns >= 1 (env resolution present).
    - `grep -c 'new URL(job.fallbackUrl).hostname' csv-writer.mjs` returns >= 1 (mirrors notify.mjs:30 fallback pattern).
    - file contains exactly 0 occurrences of `require(` (ESM only); 0 npm imports beyond node builtins.
    - file size is < 12 KB (sanity bound).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: add test/csv-writer.test.mjs (7 describe blocks, parallel-safe tmpdir fixtures)</name>
  <read_first>
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/test/notify.test.mjs (fixtures: tmpdir + randomUUID + delete env vars before import + dynamic import after env set)
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/csv-writer.mjs (the module being tested — implement Task 1 first or read it to know exact signatures)
  </read_first>
  <files>
    - /Users/andrianthan/Desktop/projects/job-board-aggregator/test/csv-writer.test.mjs
  </files>
  <action>
    create test/csv-writer.test.mjs. uses node:test + node:assert/strict + tmpdir + randomUUID. mirrors test/notify.test.mjs:11-22 fixture style.

    setup (module top-level, BEFORE dynamic import):
    - `delete process.env.CSV_PATH;` (start clean; each test sets its own.)
    - `delete process.env.DB_PATH;` (don't accidentally hit a real DB.)
    - dynamic import: `const { appendRows, parseRows } = await import('../csv-writer.mjs');`

    helper at top of file:
    - `function tmpCsv() { return join(tmpdir(), \`jobs-csv-test-\${randomUUID()}.csv\`); }`
    - `function fakeJob(overrides = {}) { return { url: 'https://example.com/' + randomUUID(), company: 'Acme Co', title: 'Finance Summer Intern', location: 'New York', source: 'greenhouse', postedAt: Date.now() - 3 * 60 * 60 * 1000, fallbackUrl: undefined, ...overrides }; }`
    - `const FIXED_NOW = new Date('2026-07-01T15:30:00Z').getTime();` — deterministic injected `now` for every assertion.

    7 describe blocks (each is autonomous, sets its own tmp path via tmpCsv()):

    1. `describe('appendRows: cold start (header-only file)', ...)` — single test: call `appendRows([], { filePath: tmpCsv(), now: FIXED_NOW })` on a missing file. assert `rowsAppended === 0` and `totalRows === 0`. read file via `readFile`. assert contents start with literal `Date Added, Company, Role, Location, URL, Source, Age, Application\n|---\n` and total `totalRows=0`.

    2. `describe('appendRows: N jobs appended exactly once', ...)` — single test: build 4 jobs, call `appendRows(jobs, { filePath, now: FIXED_NOW })`. assert `rowsAppended === 4`, `totalRows === 4`. read file; assert the file has 4 lines beginning with `| ` (data rows).

    3. `describe('appendRows: idempotent re-run produces no new rows', ...)` — two-stage test:
       - stage A: write 4 jobs to filePath.
       - stage B: call `appendRows(sameJobs, { filePath, now: FIXED_NOW })` again with same jobs.
       - assert `rowsAppended === 0` and `totalRows === 4` (unchanged).
       - assert file content is byte-identical between reads (read once into a string, read again after second call, assert `equal`).

    4. `describe('appendRows: append-only (existing rows untouched, new at bottom)', ...)` — two-stage test:
       - stage A: write jobs A,B,C to filePath.
       - stage B: append a NEW job D (URL not in file).
       - assert `rowsAppended === 1`, `totalRows === 4`.
       - parseRows(file content): assert returned array has length 4 AND the FIRST three titles (in order) match A,B,C's titles. the LAST returned row is D's URL+title.

    5. `describe('appendRows: escape rules (pipes / newlines / backticks)', ...)` — single test:
       - exercise three escape edge cases in one job:
         - `company: 'Pipe|Moon Co'` (literal pipe in the value)
         - `title: 'Line1\nLine2 Intern'` (literal newline inside title — write it as a real newline in the source string, not as `\n` escape)
         - `location: 'NYC trailing-backtick'` where the actual value is the string `NYC ` followed by exactly one backtick character (unmatched — escapeCell must balance it)
       - call appendRows. assert `rowsAppended === 1`.
       - read the file as text and apply these assertions:
         - file contains the substring `Pipe\|Moon Co` on the data row (pipe escaped to backslash + pipe sequence in the on-disk text).
         - file contains the substring `Line1 Line2 Intern` between the header line and end of the data row (newline replaced with single space).
         - count unescaped pipes in the file body: occurrences of `|` NOT preceded by `\`. a properly-formed GH-flavored row for 8 columns is `| c1 | c2 | c3 | c4 | c5 | c6 | c7 | c8 |` — 1 leading + 7 inter-column + 1 trailing = 9 unescaped pipes total. assert the data row contains exactly 9 unescaped pipes, confirming pipes inside column values are escaped (the value `Pipe|Moon Co` is emitted as `Pipe\|Moon Co` and does NOT add to the count).
       - final round-trip assertion: call `parseRows(text)` and assert the returned array has length 1, `result[0].company === 'Pipe|Moon Co'` (parser restores `\|` back to `|`), and `result[0].location` round-trips to a string whose value matches the original literal value before write (including the trailing backtick), confirming the lone-backtick balance did not corrupt the cell.

    6. `describe('appendRows + parseRows: round-trip preserves URL + title', ...)` — single test:
       - build 5 jobs with random URLs + titles.
       - call appendRows. assert `rowsAppended === 5`, `totalRows === 5`.
       - read file text. call `parseRows(text)`.
       - assert returned array has length 5.
       - for each i in 0..4: assert `result[i].url === originalJobs[i].url` AND `result[i].title === originalJobs[i].title`. set-equality on URLs: `new Set(result.map(j => j.url))` deep-equals `new Set(originalJobs.map(j => j.url))`.

    7. `describe('appendRows: missing file creates header + rows on first call', ...)` — single test:
       - confirm file does NOT exist (`fs.access` throws ENOENT — wrap in assert.throws).
       - call `appendRows([fakeJob()], { filePath, now: FIXED_NOW })`.
       - assert `rowsAppended === 1`, `totalRows === 1`.
       - assert file now exists. assert first line equals exact header literal. assert second line begins with `|---|`. assert there is exactly one `|`-prefixed data row.

    all tests are `type: 'auto'`. NO checkpoint tasks in this plan — fully autonomous.

    cleanup:
    - import `unlink` from `node:fs/promises` and `mkdtemp` etc as needed.
    - each describe block sets a fresh tmp path via tmpCsv() — no shared state. no `afterEach` cleanup needed (tmpdir is OS-cleaned), but optionally rm-rf the file in afterEach using `fs.rm(filePath, { force: true })`.
  </action>
  <behavior>
    - test/csv-writer.test.mjs contains EXACTLY 7 `describe(` blocks.
    - every test uses its own tmp path via `tmpCsv()` (no shared filePath between tests).
    - `process.env.CSV_PATH` is cleared at module load; tests inject `filePath` directly via the options arg (not via env), so test isolation is purely filesystem-level.
    - no network calls. no DB imports.
  </behavior>
  <verify>
    <automated>npm test (runs `node --test` which discovers test/*.test.mjs including the new csv-writer.test.mjs). Expected: all tests across the 11 existing suites + the new csv-writer suite pass; total test count increases by the number of `test(` blocks in csv-writer.test.mjs.</automated>
  </verify>
  <done>
    test/csv-writer.test.mjs exists at /Users/andrianthan/Desktop/projects/job-board-aggregator/test/csv-writer.test.mjs with 7 `describe(` blocks covering the D-08 cases; running `npm test` exits 0 (all 11 existing suites + the new suite green).
  </done>
  <acceptance_criteria>
    - `test/csv-writer.test.mjs` exists at /Users/andrianthan/Desktop/projects/job-board-aggregator/test/csv-writer.test.mjs.
    - `grep -c '^describe(' test/csv-writer.test.mjs` returns >= 7.
    - `grep -c 'node:test' test/csv-writer.test.mjs` returns >= 1.
    - `grep -c 'node:assert/strict' test/csv-writer.test.mjs` returns >= 1.
    - `grep -c "from '../csv-writer.mjs'" test/csv-writer.test.mjs` returns >= 1.
    - `grep -c 'tmpdir()' test/csv-writer.test.mjs` returns >= 1.
    - `grep -c 'randomUUID' test/csv-writer.test.mjs` returns >= 1.
    - running `node --test test/csv-writer.test.mjs` (or full `npm test`) exits with code 0.
    - the suite covers the 7 mandatory cases per D-08: cold start header, N-append, idempotent re-run, append-only, escape (pipe/newline/backtick), parse round-trip, missing-file cold start.
  </acceptance_criteria>
</task>

</tasks>

<verification>
  # Phase 6 plan-level verification

  after both tasks complete, the following must hold:

  1. `npm test` exits 0 — all 11 existing suites + the new csv-writer suite pass.
  2. `grep -c '^Date Added, Company, Role, Location, URL, Source, Age, Application' csv-writer.mjs` returns >= 1 (header literal present).
  3. `test/csv-writer.test.mjs` contains 7 describe blocks covering all D-08 cases.
  4. the writer module has zero internal imports (no scan.mjs, notify.mjs, db.mjs) — verified by `grep -n "from '\.\./" csv-writer.mjs` returning NO matches.

  # ROADMAP §Phase 6 success criteria mapped to acceptance:

  - "N new jobs writes exactly N new rows" -> Task 2 describe block #2.
  - "consecutive scans with same input → 0 new rows on second pass" -> Task 2 describe block #3.
  - "one new job adds one row at the bottom, prior rows untouched" -> Task 2 describe block #4.
  - "parser round-trip: reading the file back yields the same jobs in the same order" -> Task 2 describe block #6.
  - "pipe / comma / quote escape properly" -> Task 2 describe block #5 (commas do not need escaping per D-06; covered by the rule that commas are preserved verbatim while pipes are escaped).
</verification>

<success_criteria>
  - CSV-02 (CSV with 8 specific columns): header literal in csv-writer.mjs matches exactly; appendRows stamps every column in order per D-07.
  - CSV-06 (append-only, never truncated): existing-file content survives an append (describe block #4); idempotent re-run produces zero new rows and leaves bytes untouched (describe block #3).
  - CSV-09 (E2E test covers CSV append behavior, format stable, no duplicates within CSV): test/csv-writer.test.mjs has 7 describe blocks exercising round-trip, dedup, and append-only invariants — passes under `npm test`.
</success_criteria>

<output>
  After completion, create `.planning/phases/06-csv-writer-format/06-01-SUMMARY.md`
</output>
