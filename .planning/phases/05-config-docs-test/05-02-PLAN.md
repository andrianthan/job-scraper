---
phase: 05-config-docs-test
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - "README provides a complete sequential path from clone to first scheduled notification"
    - "An operator can set up and run the daemon end-to-end from only the README (no source reading required)"
    - "Every configurable value (boards, filters, env channels, schedule) is documented with an example and default"
    - ".env.example lists all four channel env vars operators need to copy"
  artifacts:
    - path: "README.md"
      provides: "Sequential operator setup path + config reference table"
      contains: "## Setup"
    - path: ".env.example"
      provides: "Template env file listing all four channel env vars"
      contains: "DISCORD_WEBHOOK_URL"
  key_links:
    - from: "README.md Setup section"
      to: "portals.config.mjs"
      via: "direct file reference with editing instructions"
      pattern: "portals\\.config\\.mjs"
    - from: "README.md Scheduling section"
      to: "docs/SCHEDULING.md"
      via: "markdown link"
      pattern: "SCHEDULING\\.md"
    - from: ".env.example"
      to: "notify.mjs env var names"
      via: "matching env var names DISCORD_WEBHOOK_URL, NOTIFY_EMAIL, RESEND_API_KEY, NOTIFY_EMAIL_FROM"
      pattern: "RESEND_API_KEY"
---

<objective>
Rewrite README.md with a sequential operator setup path and an inline config reference table, and create .env.example listing all channel env vars. An operator must be able to go from clone to first scheduled notification using only the README.

Purpose: DOCS-01 (config reference) + DOCS-02 (end-to-end operator path).
Output: README.md rewritten, .env.example created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-config-docs-test/05-CONTEXT.md

<interfaces>
<!-- Config values to document. Extracted from actual source files. -->

From portals.config.mjs — values to document in config reference table:

trackedCompanies entries:
  - Each entry: { name, careers_url, provider? (optional override), api? (optional URL), enabled? (false = parked) }
  - ATS auto-detected from careers_url; override with provider: 'greenhouse'
  - 14 enabled boards + 6 disabled (parked, no public API)

titleFilter:
  positive (title must contain ≥1):
    Intern, Internship, Summer Analyst, Summer Associate, Co-op, Co op,
    Sophomore, Freshman, First-Year, First Year,
    Spring Week, Insight Program, Insight Day, Early Identification,
    Early Insight, Discovery Program, Rising Sophomore, Rising Junior,
    Scholars Program, Fellowship, Apprentice
  negative (title must contain 0 — drop even if positive matched):
    Senior, Staff, Principal, Manager, Director, VP,
    Vice President, Head of, Engineer, Developer, Software, Data Scientist,
    PhD, Experienced, 5+ years, 10+ years, Full Time

locationFilter:
  alwaysAllow: United States, New York, Remote  (bypass block + allow)
  allow: United States, USA, US, Remote, New York, Chicago,
         San Francisco, Boston, Charlotte, Atlanta, Dallas, Los Angeles
  block: India, United Kingdom, London, Singapore, Germany, Hong Kong
  Empty/missing location always passes.

From notify.mjs — env vars to document:
  DISCORD_WEBHOOK_URL  (canonical) or DISCORD_WEBHOOK (alias) — Discord webhook URL
  NOTIFY_EMAIL         — recipient address for email digest
  RESEND_API_KEY       — Resend API key (https://resend.com)
  NOTIFY_EMAIL_FROM    — sender address (default: onboarding@resend.dev)

From docs/SCHEDULING.md — scheduling commands to reference:
  Local cron:    npm run schedule:install  (idempotent, hourly, appends to scan.log)
  GitHub Actions: already committed at .github/workflows/scan.yml
                  Add DISCORD_WEBHOOK_URL as repo secret → auto-runs hourly

From scan.mjs — CLI flags to document:
  node scan.mjs              → scan, print new jobs, update data/jobs.db
  node scan.mjs --notify     → also push notification to configured channels
  node scan.mjs --json       → also dump new jobs as JSON to stdout
  npm test                   → run test suite (node --test)
  npm run verify             → probe all boards for live slugs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with operator path + config reference table</name>
  <files>README.md</files>
  <read_first>README.md, portals.config.mjs, notify.mjs, docs/SCHEDULING.md, scan.mjs</read_first>
  <action>
    Replace README.md entirely. The new README must include these sections in this order:

    **# job-board-aggregator**
    One-paragraph pitch: finance/business/HR internship aggregator for underclassmen (sophomores/juniors).
    Fetches from public ATS APIs (Greenhouse, Lever, Ashby, Workday). Zero npm deps. Node 22+.

    **## Setup**
    Sequential steps — every step must be completable from the README alone:

    1. **Clone**
       ```sh
       git clone <repo-url>
       cd job-board-aggregator
       ```

    2. **Prerequisites**
       Node 22 or later required (uses built-in `node:sqlite` and native `fetch`).
       No `npm install` needed — zero npm dependencies.
       ```sh
       node --version   # must be ≥ 22.0.0
       ```

    3. **Edit the board list** — `portals.config.mjs`
       The file ships with 14 live boards (fintech + banks) and 6 disabled ones.
       Add, remove, or adjust entries in `trackedCompanies`. Run `npm run verify` after editing
       to drop dead slugs before relying on the list.

    4. **Configure notification channel** (optional — stdout fallback if skipped)
       Copy `.env.example` to `.env` and fill in the values you want:
       ```sh
       cp .env.example .env
       # Edit .env with your editor — add DISCORD_WEBHOOK_URL and/or NOTIFY_EMAIL + RESEND_API_KEY
       ```
       If no channels are configured, new jobs print to stdout — useful for a first dry run.

    5. **First run**
       ```sh
       node scan.mjs              # scan + print new jobs (stdout)
       node scan.mjs --notify     # scan + send digest to configured channels
       ```
       Output: `📊 scanned N · parked N · failed N · N new jobs`

    6. **Schedule** (so scans run unattended)
       See [docs/SCHEDULING.md](docs/SCHEDULING.md) for the full setup.
       Quick paths:
       - **Local cron (hourly):** `npm run schedule:install` — idempotent, adds one crontab entry
       - **GitHub Actions:** already committed at `.github/workflows/scan.yml` — add
         `DISCORD_WEBHOOK_URL` as a repo secret and push; runs hourly automatically

    **## Configuration Reference**

    Inline table (markdown) covering every configurable value:

    | Setting | File / Source | What it does | Example | Default |
    |---------|--------------|--------------|---------|---------|
    | `trackedCompanies` | `portals.config.mjs` | Companies to scan. Each entry needs `name` and `careers_url`; ATS is auto-detected. Add `provider: 'greenhouse'` to force a provider. Set `enabled: false` to park a board. | `{ name: 'Stripe', careers_url: 'https://stripe.com/jobs', provider: 'greenhouse', api: 'https://boards-api.greenhouse.io/v1/boards/stripe/jobs' }` | 14 live boards |
    | `titleFilter.positive` | `portals.config.mjs` | Title must contain **at least one** keyword. Intern-signal gate — titles without a keyword are dropped. | `['Intern', 'Summer Analyst', 'Co-op']` | 20 keywords |
    | `titleFilter.negative` | `portals.config.mjs` | Title must contain **none** of these. Drops senior / full-time roles even if a positive keyword matched. | `['Senior', 'Engineer', 'Manager']` | 14 keywords |
    | `locationFilter.alwaysAllow` | `portals.config.mjs` | Locations that bypass all other location checks. | `['United States', 'New York', 'Remote']` | US + Remote |
    | `locationFilter.allow` | `portals.config.mjs` | Locations to accept (if not already alwaysAllow). Jobs with empty location always pass. | `['Boston', 'Chicago', 'Los Angeles']` | 10 US cities |
    | `locationFilter.block` | `portals.config.mjs` | Locations to reject (checked after alwaysAllow). | `['London', 'Singapore']` | 6 non-US regions |
    | `DISCORD_WEBHOOK_URL` | env / `.env` | Discord channel webhook. When set, `--notify` posts the digest as embeds. Alias: `DISCORD_WEBHOOK`. | `https://discord.com/api/webhooks/123/abc` | none (stdout fallback) |
    | `NOTIFY_EMAIL` | env / `.env` | Recipient email address for digest emails. Requires `RESEND_API_KEY`. | `you@example.com` | none |
    | `RESEND_API_KEY` | env / `.env` | Resend API key for email delivery ([resend.com](https://resend.com)). | `re_AbcXyz123` | none |
    | `NOTIFY_EMAIL_FROM` | env / `.env` | Sender address for email notifications. | `scanner@yourdomain.com` | `onboarding@resend.dev` |
    | Schedule interval | cron / `.github/workflows/scan.yml` | How often the scan runs unattended. | `0 * * * *` = every hour | hourly |
    | `DB_PATH` | env | Path to the SQLite dedup database. Auto-created on first run. Override in tests. | `data/jobs.db` | `data/jobs.db` |

    **## How it works** (brief, after the setup sections)
    Diagram of: portals.config.mjs → scan.mjs → providers/*.mjs → filter → dedup (data/jobs.db) → notify.mjs → channels.
    Note: Zero-token — never fetches individual job pages, never calls an LLM.

    **## Adding a board**
    Two lines: (1) add entry to trackedCompanies, (2) run `npm run verify` to confirm the slug is live.
    Mention that 19 providers ship in providers/ — adding a new ATS = drop a providers/x.mjs
    exporting `{ id, detect, fetch }`.

    **## Seasonality note**
    Postings for a given summer open the prior fall. Near-zero results in spring/summer for
    next-summer roles is expected, not a bug.

    **## Attribution**
    Provider modules, _http.mjs, role-matcher.mjs © career-ops contributors, MIT.

    Keep the README under ~120 lines. No source-code spelunking required to follow the Setup section.
    Use concrete commands with exact flag names. Do not use vague phrases like "configure appropriately."
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const md = fs.readFileSync('README.md','utf8'); const checks = ['## Setup', '## Configuration Reference', 'portals.config.mjs', 'SCHEDULING.md', 'DISCORD_WEBHOOK_URL', 'NOTIFY_EMAIL', 'RESEND_API_KEY', 'node scan.mjs --notify', '.env.example']; const missing = checks.filter(s => !md.includes(s)); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('README OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '## Setup' README.md` exits 0
    - `grep '## Configuration Reference' README.md` exits 0
    - `grep 'portals.config.mjs' README.md` returns ≥3 lines (setup + table + adding boards)
    - `grep 'SCHEDULING.md' README.md` exits 0 (link to scheduling docs)
    - `grep 'DISCORD_WEBHOOK_URL' README.md` exits 0 (in config table)
    - `grep 'NOTIFY_EMAIL' README.md` exits 0 (in config table)
    - `grep 'RESEND_API_KEY' README.md` exits 0 (in config table)
    - `grep '\.env\.example' README.md` exits 0 (referenced in setup step 4)
    - `grep 'node scan.mjs --notify' README.md` exits 0 (first-run command)
    - `grep 'schedule:install' README.md` exits 0 (scheduling step)
  </acceptance_criteria>
  <done>README.md rewritten with complete operator path (clone → notify) and config reference table; no source reading required to follow setup</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example</name>
  <files>.env.example</files>
  <read_first>notify.mjs</read_first>
  <action>
    Create .env.example at the project root with this exact content
    (comments explain each var; operators copy to .env and fill in values):

    ```
    # job-board-aggregator — notification channel config
    # Copy this file to .env and fill in the values you want to use.
    # At least one channel (Discord or email) must be configured for --notify to send.
    # If neither is set, new jobs print to stdout instead (useful for dry runs).

    # Discord: paste your webhook URL from Server Settings → Integrations → Webhooks
    DISCORD_WEBHOOK_URL=

    # Email via Resend (https://resend.com — free tier: 100 emails/day)
    # NOTIFY_EMAIL = recipient address
    # RESEND_API_KEY = API key from the Resend dashboard
    # NOTIFY_EMAIL_FROM = sender address (default: onboarding@resend.dev if omitted)
    NOTIFY_EMAIL=
    RESEND_API_KEY=
    NOTIFY_EMAIL_FROM=
    ```

    Also verify .gitignore (or create if missing) has `.env` (not `.env.example`) listed
    so the filled-in file with real secrets is never committed.
    Check: `grep '^\.env$' .gitignore` — if not found, append `.env` on its own line.
    `.env.example` itself should NOT be in .gitignore (it's safe to commit as a template).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const env = fs.readFileSync('.env.example','utf8'); const vars = ['DISCORD_WEBHOOK_URL', 'NOTIFY_EMAIL', 'RESEND_API_KEY', 'NOTIFY_EMAIL_FROM']; const missing = vars.filter(v => !env.includes(v)); if (missing.length) { console.error('MISSING vars:', missing); process.exit(1); } console.log('.env.example OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'DISCORD_WEBHOOK_URL' .env.example` exits 0
    - `grep 'NOTIFY_EMAIL=' .env.example` exits 0
    - `grep 'RESEND_API_KEY=' .env.example` exits 0
    - `grep 'NOTIFY_EMAIL_FROM=' .env.example` exits 0
    - `grep '^\.env$' .gitignore` exits 0 (secrets file excluded from git)
    - `.env.example` is NOT listed in .gitignore (template is safe to commit)
  </acceptance_criteria>
  <done>.env.example created at project root with all four channel env vars; .gitignore guards .env (not .env.example)</done>
</task>

</tasks>

<verification>
```bash
# README content checks
grep '## Setup' README.md
grep '## Configuration Reference' README.md
grep 'SCHEDULING.md' README.md
grep 'DISCORD_WEBHOOK_URL' README.md

# .env.example checks
grep 'DISCORD_WEBHOOK_URL' .env.example
grep 'RESEND_API_KEY' .env.example
grep 'NOTIFY_EMAIL_FROM' .env.example

# .gitignore guard
grep '^\.env$' .gitignore

# Operator path completeness: trace setup steps without reading source
grep -n 'node scan.mjs' README.md         # first-run command present
grep -n 'schedule:install' README.md      # scheduling step present
grep -n '\.env\.example' README.md        # .env.example copy step present
```
</verification>

<success_criteria>
- README.md has ## Setup and ## Configuration Reference sections
- README.md links to docs/SCHEDULING.md
- README.md documents all env vars (DISCORD_WEBHOOK_URL, NOTIFY_EMAIL, RESEND_API_KEY, NOTIFY_EMAIL_FROM)
- .env.example exists with all four vars
- .gitignore excludes .env (secrets) but not .env.example (template)
- An operator can follow the README from clone to first scheduled notification without opening any source file
</success_criteria>

<output>
After completion, create `.planning/phases/05-config-docs-test/05-02-SUMMARY.md`
</output>
